{
  "name": "cothink",
  "version": "1.0.0",
  "description": "CoThink — an ethical human-AI collaboration workspace. The AI proposes, the human disposes: every suggestion carries provenance (model, uncertainty, bias flags) and needs explicit human approval before it can apply. No autonomous actions.",
  "private": true,
  "main": "server.cjs",
  "scripts": {
    "test": "node --test",
    "start": "node server.cjs"
  }
}