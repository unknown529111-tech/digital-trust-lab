{
  "name": "cothink",
  "version": "1.0.0",
  "description": "VeriLoop (evolved from CoThink) — an evidence-in-the-loop AI workspace. The AI proposes, a verification layer decomposes the answer into claims and grades each one with a cross-check model, contradictions are surfaced, and the engine refuses any human decision on an unverified suggestion. No autonomous actions.",
  "private": true,
  "main": "server.cjs",
  "scripts": {
    "test": "node --test",
    "start": "node server.cjs"
  }
}